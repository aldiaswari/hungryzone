jobs:
  example-job:
    docker:
      # replace with your preferred image
      - image: cimg/base:stable
    steps:
      - browser-tools/install-chrome
workflows:
  example-workflow:
    jobs:
      - example-job